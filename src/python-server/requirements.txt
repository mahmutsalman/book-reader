# Core server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Text-to-Speech (Offline Neural Voices)
piper-tts>=1.2.0

# IPA Transcription
gruut>=2.3.0

# PDF Processing
PyMuPDF>=1.23.0
pytesseract>=0.3.10
Pillow>=10.0.0
scipy>=1.10.0

# Note: OCR engines (PaddleOCR, EasyOCR) are NOT included in core dependencies.
# Users install them on-demand via Settings UI to reduce initial download size.
# OCR packages install to user data directory: ~/Library/Application Support/BookReader/ocr-packages
